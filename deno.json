{
  "workspace": ["./core", "./server"],
  "tasks": {
    "//": "db/ se comprueba con su propia config: está fuera del workspace (ver db/deno.json) y desde aquí no se resuelve su mapa de importaciones.",
    "check": "deno check core server && deno task --cwd db check",
    "lint": "deno lint core server && deno task --cwd db lint",
    "fmt": "deno fmt --check core server db",
    "test": "deno test -A core server",
    "dev": "deno run -A --watch server/src/main.ts",
    "db:generate": "deno task --cwd db generate",
    "db:migrate": "deno task --cwd db migrate"
  },
  "fmt": {
    "lineWidth": 100,
    "exclude": ["webapp/", "db/migrations/", "db/node_modules/"]
  },
  "lint": {
    "rules": {
      "tags": ["recommended"],
      "include": ["no-sync-fn-in-async-fn", "no-throw-literal", "eqeqeq"]
    }
  },
  "exclude": ["webapp/", "db/node_modules/", "db/migrations/"]
}
